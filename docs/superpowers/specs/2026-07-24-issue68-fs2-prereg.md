# #68 신규 후보 F-S2 사전등록 — 매출 조건 완전형 (2026-07-24)

> **상태: DRAFT** — 사용자 게이트 승인 시 LOCKED. 준거·상속: 3단계 사전등록
> `2026-07-22-issue68-stage3-filter-prereg.md`(LOCKED — 이하 "모문서")의 §2
> 공통 정의 전부(3개월 단독·4Q 파생·턴어라운드 4라벨·결산기 이행기·판정불가
> 3그룹). 본 문서는 모문서의 "변경 시 신규 후보 취급 + look 가산" 조항이
> 허용하는 **1회 재도전**이며, 모문서의 정의·수치는 일절 변경하지 않는다.

## 1. 등록 동기 (4단계 관찰 결과 — 수치 확정분)

- 4단계 탐색(`2026-07-22-issue68-stage4-exploration.md`): 승격 0건. 최근접 =
  **F-S1(매출 YoY ≥+25%) +7.6%p** — 기준 +8.0%p 에 0.4%p 미달.
- F-S1 은 오닐 매출 조건의 **절반 구현**이었다(모문서 §8 gap 2-1 등록분):
  원문은 "최신 분기 매출 ≥+25% **또는** 최근 3분기 매출 증가율 가속"의
  이중 지선 [HMMS C장]. 미구현 지선(가속)을 채운 완전형이 book-mandated
  후보로 남아 있어, 이를 **신규 후보 F-S2** 로 등록한다.
- 주의(정직성): F-S1 의 +7.6%p 를 본 뒤 그 계열을 재등록하는 것이므로
  데이터 주도 선택이다 — 그래서 채택이 아니라 **A+B 관찰(look 가산) →
  표본 C 판정**의 2단 경로를 강제한다.

## 2. F-S2 정의 (잠금 대상)

- **F-S2** = 지선 ① 최신 분기 `revenue` YoY ≥ **+25%** (모문서 F-S1 그대로)
  **OR** 지선 ② **3분기 연속 가속**: g_t > g_{t−1q} > g_{t−2q}
  (최근 3개 분기 각각의 YoY 성장률이 단조 증가 — 4Q 파생 포함).
- 지선 ② 는 g 3개가 전부 정의(기준값 > 0)될 때만 평가 — 하나라도
  턴어라운드/미정의면 지선 ② 판정불가.
- 라벨 합성: 지선 중 하나라도 pass → **pass** / 둘 다 fail → **fail** /
  지선 ① turnaround 이고 지선 ② 가 pass 아님 → **turnaround** /
  그 외(판정불가 혼재) → **indeterminate**.
- 데이터 경로·가드: 모문서 §2 그대로(매출은 금액 기반 — 주식수 가드 비적용).
  +25% 임계·모든 공통 규칙 변경 없음.

## 3. 판정 경로 (2단 — 잠금 대상)

1. **A+B 관찰 (look #9, 기존 8 에 가산)**: 4단계와 동일 모집단(149 트레이드,
   동일 산출 경로·seed 20260722)에서 F-S2 라벨링. **승격 기준 = 모문서 §4
   동일**(pass ex-turnaround n ≥ 25 · 승률차 ≥ +8.0%p · 판정불가 ≤ 40%).
   - 미달 시: **트랙 종결**(F-S 계열 추가 변형 재등록 금지 — 본 문서가
     허용된 1회 재도전임). 결과는 관찰 기록으로 남긴다.
2. **표본 C 판정 (look #10)**: 관찰 승격 시에만. 채택 기준 = 표본 C 트레이드
   셋에서 동일 3기준의 **재현**(pass n ≥ 25 · 승률차 ≥ +8.0%p · 판정불가
   ≤ 40%). 충족 시 실전 채택 후보로 상신(구현·wiring 은 별도 이슈), 미충족
   시 트랙 종결. 기준은 지금 잠그며 표본 C 관찰 전 변경 금지.

## 4. 표본 C 의존성 (선행 작업 — 판정과 분리)

- 표본 C 트레이드 셋은 **#52 본실행**(LLM 분류 백필, 별도 비용 승인 트랙)의
  산출물 — 본 트랙은 그 완료를 기다린다(트레이드 생성에 관여하지 않음).
- DART 실적: 표본 C 100종목 백필 필요 + **연도 하한 연장**(C 구간
  2017-H2~2020 의 YoY 는 2016년 분기 필요 — 백필 YEARS 를 C 표본에 한해
  2016 부터로 확장, 무료 API ~5,000콜). eps-published 패스 포함. 이 작업은
  트레이드·성과와 결합되지 않으므로 판정 전 수행 가능(데이터 준비).
- 해석 가드 상속: 관찰·판정 각각 귀무 하 통과 확률이 낮지 않음(모문서 §4) —
  표본 C 재현까지 가야 증거로 취급한다.

## 5. 실행 잠금 체크리스트

- [ ] 사용자 게이트 승인 → 본 문서 LOCKED 플립
- [ ] A+B 관찰 실행(결정론, LLM 0콜) → 결과 부록 기록
- [ ] (승격 시) 표본 C DART 백필 준비 + #52 완료 대기
- [ ] (표본 C 트레이드 확보 시) 판정 실행 → 채택/종결
