# #118 사이클 1 — 상폐 게이트 지표 산출 + #115 리콜 붕괴 설계 (v1)

> 6차(#115) ②-(a) 이행: 상폐 c1~c7 산출은 리콜 구간 붕괴와 #118 재분석
> 묶음(marginal 측정 등)의 공통 병목. 전부 탐색 등급·LLM 0·로컬 DB 전용.

## 1. 산출 정의 — 현행 함수 재사용(복제 금지)

- 대상: 상폐 435종목 (`delisted_adj_prices` v5-d). carve 5종목은 c8 부재
  (bt_rs 제외됨)로 게이트 미판정 — 명시 보고.
- **adj_high/low 유도**: 일자별 계수 = `adj_close/raw_close` 를 raw high/low
  에 적용(production 수정 고저가와 동형 — 동일 계수 원칙). halt(0값) 행 제외.
- c1~c7: `compute_minervini_c1_to_c7` — sma(50/150/200), `w52_high_low`
  (252, min_periods=240) 전부 현행 함수 그대로.
- rs_line_not_declining_7m: `aggregate_to_weekly` → `compute_rs_line`(KOSPI
  1001 비수정 주봉 종가 분모) → `compute_rs_line_not_declining`(30주) →
  daily backward 미러(Phase D 동형).
  **한계 명기**: index_daily 1001 은 2016-06-13~ — 그 이전 상폐 구간 RS line
  = NaN → 게이트 미충족 취급(production 동형). 2017 초 anchor 일부 영향.
- c8: `bt_rs_daily.rs_rating ≥ 70` (기산출).
- 무편향 게이트 = c1~c7 AND rs_line_not_declining_7m AND c8.

## 2. 격리 산출 표면

```sql
CREATE TABLE bt_delisted_indicators (
  ticker VARCHAR(10) NOT NULL, date DATE NOT NULL,
  c1 BOOLEAN, c2 BOOLEAN, c3 BOOLEAN, c4 BOOLEAN, c5 BOOLEAN,
  c6 BOOLEAN, c7 BOOLEAN, rs_gate BOOLEAN, c8 BOOLEAN,
  gate_pass BOOLEAN, PRIMARY KEY (ticker, date)
);
```
백테스트 전용 — 라이브 무접촉. `delisted_adj_prices` 소비 한정 해제 3호
필요(승인 소비자 목록에 본 산출 러너 추가 — 7차 확인 항목).

## 3. #115 리콜 구간 붕괴 (6차 ③ 조항 이행)

- 상폐 승자 에피소드 511건을 §1 게이트·5차 봉인 윈도우((anchor−28, anchor)
  개구간)로 판정 → 리콜 점 추정.
- **스냅샷 1회 + 일자 각인**(연속 갱신 없음), 말단 18~24개월(2025~26 상폐
  승자 7·1건) **절단 주석 상시 부착**.
- carve 5종목·RS NaN 구간 에피소드는 잔여 미판정으로 잔존 구간 표기(0 이
  아니면 헤드라인 라벨 유지).

## 4. 리드타임 분포 (6차 ②-(b) 승인 descriptive)

- 에피소드별 첫 게이트 통과일 탐색 구간 = [anchor−28일, anchor+91일(13주)].
- lead = (첫 통과일 − anchor)일 (음수 = 개시 전 포착). 미포착 = 구간 내 통과 0.
- 중간 포착의 **잔여 시장초과** = 첫 통과일→(anchor+13주 시점) 종목수익 −
  동일구간 시장수익 [descriptive — 판정 불입력].

## 5. 7차 결정 요청 (측정 전 봉인 대상 — 승인 전 산출 금지)

1. **미포착 분해 정의**: 의도된 포기(Stage 1) = 윈도우 내 구조 조건(c1~c5)
   실패 ≥ 2개 / **경계 탈락** = 실패가 단일 조건이거나 c8 단독 실패이며
   rs_rating ≥ 60 [design-judgment 초안 — 자구 확정 요청].
2. **중간 포착 유효율 지표 승격**(사용자 제기): 에피소드 진행 중 첫 통과
   시점의 잔여 시장초과 ≥ +20%p(값 합의) 비율 — descriptive 에서 판정 입력
   으로 승격할지, 승격 시 지위 문구.
3. 소비 한정 해제 3호(§2) 확인.

## 5.5 7차 결정 봉인 (2026-08-23 — 산출 전 문서화·중도 변경 금지)

- 기록: 구간 붕괴 15.89%(상폐 13.5 vs 생존 16.0) = **측정 정확도 개선으로
  기록**(성능 악화 아님).
- **① 미포착 분해 — MECE 수정판**(초안 반려·대체):
  - 평가 시점 = 창 내 **통과 조건 수(c1~c8 기준) 최대 일**, 동수 → anchor
    최근접 일. [집계·분류 모두 c1~c8 도메인 — rs_gate 실패는 C 구성비에 표기]
  - **A 의도된 포기** = c1~c5 실패 ≥ 2
  - **B 경계 탈락** = ¬A ∧ 총 실패(c1~c8) = 1 ∧ (실패가 c8 이면 rs≥60)
  - **C 비경계 잔여** = 나머지 전부(c6+c7 동시, c8+기타, c8 단독 rs<60,
    평가 불능 포함 — 구성비 보고)
  - 검산 의무: A+B+C = 미포착 전수·상호 배타(보고서 합계 검산 명기).
  - **B 내부 의무 보고**: 실패 조건 구성비 + **임계 대비 마진 분포**
    (임계 조정 논의의 실질 입력 = B 마진 분포, B 총량 아님).
  - rs≥60 은 분류 전용·파이프라인 비입력 [design-judgment].
- **② "잔여 런웨이 충분 포착률" 조건부 승격** [사용자 제기 지표 공식화]:
  - 지위 문구: "기준선 관측 — 합격 임계 없음. 용도 = 재측정 비교·방향 일관성"
  - 라벨 강제: **"필터 상태 기준 상한 — 운영 포착 아님"** (개시 전 지표에도 대칭)
  - 1차 = 시장초과 ≥ +20%p(승자 정의 도메인 일치) + 절대 ≥ +20% descriptive
    병기(앵커 상수와의 차원 차이 주석). 책 근거: base-on-base·다중 베이스.
- ③ 소비 한정 해제 3호 승인 — 목록 = {RS 재계산, P1 러너, 상폐 게이트 러너}.
  상폐 c1~c7 테이블도 bt_* 격리 + 소비 규칙 등재.

## 5.6 8차 결정 봉인 (2026-08-24 — 이행 전 문서화)

- **A 해석 채택 승인 — #115 클로즈 문안 조건**: ① 한정구 "봉인된 분해 정의
  기준"(독립 Stage 감사 아님) + 이중 태그(의도 = book-mandated(Stage 2 한정) /
  조작화 = design-judgment) ② 라벨 "구조 미형성(Stage 1/4류)"(Stage 1 단독
  표기 불가) ③ 전체 승자 환산 산술 명기: A ≈66.7%p / B ≈6.1%p / C ≈11.2%p,
  **임계 조정 회수 상한 = +6.1%p(B 전량 가정) + B 마진 중앙 비면도날 →
  실회수 그 이하**(개시 요건 ③ 산출물) ④ C 의 rs_gate 단독 131 = 데이터
  가용성 실패 분리 표기(임계 무관·처방 상이) + **원인 분포(워밍업 vs 결손)
  저장본 1회 분류**. 승계: 6차 첫 줄 한정구·런웨이 라벨·재현 차이·31건 주석.
- **재실행 순서 승인**: #111 → #117 → marginal 4변수 (2017~24 고정). 조건:
  ① 재실행 = **대체 측정**(신규 버전 라벨 + 원판정 "superseded" 표기, 소급
  수정 없음, 방향 뒤집힘 명시 기록) ② **재실행별 예상 방향 1줄 사전 봉인**
  (위반 → 채택 전 버그 조사) ③ marginal 4변수는 판정 기준(3%/3개 수정 발의
  조건) 사전등록 = 실행 개시 조건, 실행 직전 기준 초안 세션 회부.
- 임계 조정 사이클 잠금 유지(④⑤ 미수립). ④⑤ 초안은 병렬 작성 가능(실행 별개).

### 재실행 예상 방향 사전 봉인 (조건 ② 이행)

- **#111 재실행 예상**: 원판정 방향(전환 즉시매수·무손절 20일 보유 = 역효과)
  **유지 또는 악화** — 생존편향 제거는 약세(상폐 예정) 종목의 전환 표본을
  추가하므로 평균 초과수익이 더 낮아지는 방향. [사전 봉인 2026-08-24]
- **#117 재실행 예상**: "반복 전환이 평균을 지배(첫 전환만 +는 미입증)" 구조
  유지 — 상폐 편입은 반복 전환 꼬리를 두껍게 하는 방향. [사전 봉인 2026-08-24]

## 5.7 10차 판정 봉인 (2026-08-24)

- 버그 조사 승인 — **"봉인이 새 정보를 강제 발견시킨 사례 1호"** 기록.
- 원판정 처리: **"superseded by r111-v2 (데이터 교체 + 기간 이질성 발견)"**.
- **공식 서술(자구 고정)**: "전환 후 초과수익은 부호 안정적이지 않음 —
  2017~24 +0.35 / 2025~26 -2.46 [-3.10, -1.82]. 통합 부호는 측정창 구성의
  함수." — "역효과→유효 반전" 서술 금지.
- **인용 금지 조항**: r111-v2 +0.333 을 "전환 신호 유효"의 독립 근거로 인용
  불가(qualifier 지위 불변 + 사후 기간 선택).
- 신규 등록: 2025~26 음수 효과(CI 0 분리, 유일 '입증' 구간) — 가설 (a) 국면
  구성 (b) 전환 밀도/경계 진동 변화 (c) 구조 변화. 선행 권장(저비용·저장
  로그) = 해당 구간 M-gate 실전 차단률(방어지표 3회 재현 연결). 운영 함의
  없음(전환 성과 = 파이프라인 비입력). **원인 규명 우선순위 = 사용자 결정**.
- **r117-v2 지침**: 기간 분해(2017~24 / 2025~26) 사전 보고 항목 승격. 봉인
  방향("반복 전환 지배 유지")은 통합·기간별 각각 판정.

## 5.8 11차 판정 봉인 (2026-08-24)

- **r117-v2 채택·supersede 확정.** 서술 보강: ① "최악"·단조 순서 = 점추정
  라벨("구간 중첩 — 차이 미입증" 병기, n 명기) ② 코호트 구성 주석(2025~26
  첫 전환 = 신규상장 30% + 장기 미통과 70% — 동일 지표·상이 코호트)
  ③ 인용 금지 조항을 +1.26/+0.584 에 승계.
- 위반 블록: 절차 = 점검 완료 종결(**트립와이어 정상 작동 2호**). 실질 =
  #125 증거 이관 — 가설 (d) 신규 편입 코호트 취약 추가. (a) 국면 vs (d)
  코호트는 층화 재분석으로 판별 가능. M-gate 차단률 선행 권장 유지.
- **marginal 발의 기준 — 수정판 봉인(자구)**:
  발의 성립 = ①′ 순효과(≡ 이탈군 회피손실 − 해소군 지연비용) < 0
  [지연비용 1차 = 차단 트리거 가상 성과, 드리프트 = 보조]
  AND ②′ 강등 발화의 첫 전환 편중 재현(층화 의무).
  발의 표적 = 편중 ≥50% → 해당 조건 마진 정의 / 고른 분포 → mc 임계(3%/3개)
  [50% = design-judgment, 라우팅 경계].
  불변: 발의 ≠ 변경(확증 + threshold-change-checklist) / 결과 관측 후 기준
  자구 변경 금지 / 측정창 2017~24 고정. **세 수량(순효과·편중도·역선택도)
  무조건 전부 보고.** → 4변수 실행 개시 조건 충족.

## 5.9 12차 판정 봉인 (2026-08-24)

- 4변수 접수 — **발의 없음, mc(3%/3개) 유지 확정**. 역선택 종결문(자구):
  "표적 편중 실재(78.0 vs 43.7), 순효과 양(+1.821) → 강등의 첫 전환 편중은
  손실 회피로 정당화됨". downtrend 발화 57.1% 방어 계보 정합. 모집단 생존
  한정 주석 — 상폐 제외는 회피손실 과소 방향 = '순효과>0' 에 보수적
  (결론 강건, 재실행 불요).
- **차단 트리거 프록시 조건부 승인** [design-judgment — 사전 봉인 누락 인정].
  완결 조건: **이웃 격자(신고가 40/90일 × 거래량 1.25/2.0×) 순효과 부호
  강건성 descriptive 보고** — 전 셀 부호 유지 → 봉인 완결 / 반전 → 판정
  보류 + 세션 회부.
- **recent_transition_count_63d 자문 입력 채택.** 조건: ① 중립 노출(정의+
  해석 프레임까지, 지시어 금지 — 가중치 LLM 재량, 저성과 가설은 탐색 등급·
  지시 승격 금지) ② 1버전 1변경 + 전후 비교 재실행 무효(저장본 드리프트
  관찰만 — 판정 아님) ③ 태그 design-judgment + "예측력 미확증(탐색 유래)"
  + 하드 게이트 승격 재논의 시 순환성 가드 병기 ④ **구현 별도 사이클 —
  변경 지점 맵(payload/프롬프트/store 검증) 선제출**.

## 5.10 13차 판정 봉인 (2026-08-24)

- **프록시 격자 봉인 완결 승인** (4셀 부호 유지, 본측정 재현 확인).
  descriptive 기록 1건: **프록시 엄격화 → 순효과 증가 단조 경향**
  (hi40/vol1.25 +1.482 → hi40/vol2.0 +1.913 · hi90/vol1.25 +1.920 →
  hi90/vol2.0 +2.267, 중심 +1.820 — 양축 단조). 의미 = 판정의 프록시
  비의존성 강화. **신규 판정 아님(descriptive)**.
- **rtc_63d 변경 지점 맵 승인.** 구현 전 확인 3건:
  ① F→T 전환 정의 = #117 이벤트 정의와 **자구 동일** — docstring 에 계보
  참조 명기.
  ② 관측창 <63거래일 → **None (0 아님)** — 신규 상장의 '안정 통과' 위장
  방지, 경계 테스트 포함. #125 가설(d) 접점.
  ③ 프롬프트 해석 프레임 1문장은 **배포 전 자구만 세션 회부**(타 항목
  재회부 불요).
- **#118 클로즈 문안 필요 판정** — 골격 6항(무편향 재측정+supersede /
  2025~26 → #125 이관 / marginal 종결+격자 완결 / rtc_63d 채택 / 불변
  조항 / 오류 원장 색인). 조건: **전 수치·자구 = 기봉인 차수 인용, 신규
  해석·수치 도입 금지 — 클로즈 = 색인, 판정 아님.**

## 6. 게이트

- [ ] §1~4 산출·보고 (승인 불요 — 6차 기승인 범위)
- [ ] §5 7차 승인 → 미포착 분해·유효율 산출
- [ ] 이후: #118 체크리스트 재분석(#111·#117 재실행, marginal 4변수 —
      2017~24 고정, mc 튜닝 금지 조항 유지)
