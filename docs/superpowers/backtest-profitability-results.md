# 수익성·강건성 백테스트 — 결과 (§7 판정 + §8 한계)

실행일: 2026-07-02 · 도구: `python -m kr_pipeline.backtest.profitability_cli analyze` (읽기전용·결정론)
입력: `backtest_classification` 동결 100종목(사전등록 `backtest-profitability-sample.md`) × 2021–2024 주간 분류 2,135점(lockdown 후 적재, C=0 완전 커버)
스펙: `specs/2026-06-23-profitability-robustness-backtest-design.md` — 판정 기준은 결과 산출 **전** §7에 박아둔 그대로 적용.

## ⚠️ 범위 라벨 (먼저 읽을 것)

핵심 질문은 "얼마 버나"가 아니라 **"하락·조정기에 게이트가 진입을 억제하나"**(§0).
트레이드 층은 **결정론 트리거의 하한(lower-bound) 추정**이다 — LLM 트리거 확인·포지션
사이징 제외, `breakout_from_watch`가 시장 비게이팅이라 **하락기 진입을 과대평가**(§4,
보수적 방향). 게이트 방어의 1차 판정은 트리거 근사에 오염되지 않는 **분류층**(§7.1)이다.

## 입력 분류 (동결 100종목, 2,135 분류점)

| verdict | 건수 | 비고 |
|---|---|---|
| watch | 2,014 (94.3%) | actionable 매수는 watch pivot 의 트리거에서 발생 |
| ignore | 120 (5.6%) | |
| **entry** | **1 (0.047%)** | 012450, 2024-06-29 — confirmed_uptrend 국면(§3.5 정합) |

국면별 분류점: rally_attempt 768 · correction 818 · downtrend 257 · confirmed_uptrend 292.

## §7.1 게이트 방어 (1차, 사전등록) — **PASS**

| 지표 | 값 |
|---|---|
| R_down = entry-rate(downtrend ∪ correction) | **0 / 1,075 = 0.0** |
| R_up = entry-rate(confirmed_uptrend) | 1 / 292 = 0.0034 |
| R_down / R_up (기준 ≤ 0.5) | **0.0 → PASS** |

하락·조정기 분류점 1,075개에서 `entry` 분류 **정확히 0건**. §3.5 hard rule 이 스펙의
기대("R_down은 0에 가까울 것") 그대로 작동했고, 유일한 entry 1건도 confirmed_uptrend
에서 나왔다. **게이트의 하락기 진입-분류 억제 = 입증.**

읽기 주의: entry verdict 자체가 극도로 희소(1/2,135)하므로 이 판정은 "직접-entry 경로의
방어"다. 실제 매수 노출은 watch→트리거 경로가 지배하며, 그 경로의 하락기 노출은 아래
§7.2 트레이드 층(비게이팅 근사)으로 본다.

## §7.2 손실 억제 — 시장대비 초과수익 (보조) — **supportive 아님 (게이트 실패 아님)**

트레이드 66건(동결 100종목 production 시뮬, 진입일 국면 라벨):

| 국면 | n | 평균 초과 | 중앙 초과 | 승률(초과>0) | 평균 P&L |
|---|---|---|---|---|---|
| downtrend | 9 ⚠️underpowered | **+25.6%** | −2.5% | 44.4% | +23.1% |
| correction | 24 | **−7.3%** | −6.4% | 16.7% | −6.6% |
| rally_attempt | 21 | +7.3% | −3.9% | 23.8% | +5.8% |
| confirmed_uptrend | 12 | −3.3% | −8.6% | 25.0% | −2.5% |

- 사전등록 기준(하락∪조정 평균 초과 ≥ 0)은 **correction −7.3%로 미충족** → supportive
  아님. 사전등록 오판-방지 조항 그대로: **§7.2 음수 ≠ 게이트 실패** — 이 트레이드들은
  시장 비게이팅 트리거(§4)가 강제한 진입을 포함하므로 "결정론 근사의 하한 손실"로
  해석하고, 게이트 판정은 §7.1(분류층, PASS)로 한다.
- 하락·조정기 트레이드 33건(66의 50%)은 전부 watch pivot 의 `breakout_from_watch`
  발화 — 실제 시스템은 LLM 트리거 확인에서 §3.5를 재적용하므로 실전 하락기 노출은
  이보다 적다(하한 방향).
- downtrend 평균 +25.6%는 대형 아웃라이어 2건(004360 2022-10 진입 +159.9%,
  053350 2025-01 진입 +99.6% — 약세장 바닥 진입 후 회복 편승)이 견인. 이 2건 제외
  평균 −4.1%, 중앙값 −2.5%. n=9 < 10 이라 **§7.5 underpowered — 과해석 금지**.
- correction 24건의 분포는 방어적 실패 패턴이 아니라 whipsaw 패턴: 24건 중 20건이
  −22%~−1% 구간의 통제된 손절, 최악 1건 −46.9%(077500, base_low 청산).

## §7.3 상승 기준선 (정보용)

confirmed_uptrend 평균 초과 −3.3%(n=12, 중앙 −8.6%) — 12건 중 승 3건(최대 012450
+38.2%). 2024 8종목 백테스트(트리거 층 4트레이드 평균 초과 ≈ +146%)와의 직접 비교는
무의미(그쪽은 큐레이션 8종목 존재증명, 이번은 무작위 100종목 측정). 합격선 없음.

## §7.4 횡보 whipsaw (정보용)

rally_attempt 21건: 승률 23.8%, 중앙 초과 −3.9%, 아웃라이어 1건(016710 2021-04 진입,
2년 보유 +222.1%) 제외 평균 −3.4%. **잦은 소손절 + 드문 대형 승리**의 전형적
추세추종 분포 — 가짜 돌파 손절 비용은 실재하나 개별 손실이 대부분 한 자릿수로 통제됨.

## §7.5 검정력 가드

downtrend 트레이드 9건 < 10 → **underpowered 표기**(사전등록대로 기준 불변경).
나머지 국면(21·24·12)은 ok. 전체 66건: 승률 24.2%, 평균 초과 +2.6%, 중앙 초과 −5.6%
— 평균은 우측 꼬리 3건(+222/+160/+100)이 지배, 수익성 주장에는 표본 부족(이 백테스트의
목적 아님).

## 민감도 분석 — 드리프트 종목 포함 여부 (결론 강건성)

표본 드리프트 사고(§2 위반, 2026-06-30 발견·수정)로 동결 밖 14종목 165행이 적재돼
있다. 주 결과는 사전등록 동결 100(위). 민감도로 **14종목 포함(114종목, 2,300분류점,
70트레이드)** 재분석:

| 지표 | 동결 100 (주 결과) | +드리프트 14 (민감도) |
|---|---|---|
| R_down | 0 / 1,075 = 0.0 | 0 / 1,130 = 0.0 |
| R_up | 0.0034 | 0.0033 |
| **§7.1 판정** | **PASS (ratio 0.0)** | **PASS (ratio 0.0)** |
| correction 평균 초과 | −7.3% (n=24) | −7.5% (n=25) |
| rally_attempt 평균 초과 | +7.3% (n=21) | +4.9% (n=24) |

**판정·부호·규모 모두 불변 → 결론은 표본 드리프트 처리 방식에 강건.** (동결 100 내부는
stale 0 — A=qualifying=2,135, C=0 실측.)

## §8 한계 (사전 명시분 재확인)

1. **결정론 트리거·청산 = 하한 추정** — LLM 트리거 확인·사이징 제외.
   `breakout_from_watch` 시장 비게이팅으로 하락기 진입 과대평가(§7.2가 그 영향권).
2. **입력 분류 = 1회 LLM 백필** — 비결정성 때문에 재실행 비교 금지, "그 입력 고정
   하의" 결과.
3. **생존 편향 잔존** — 상폐 종목 부재(DB delisted_at 전무), 결과가 부풀려지는 방향.
   우측 꼬리(+222% 등)는 특히 이 편향의 수혜 가능.
4. **진입일-기준 국면 라벨** — 보유 중 국면 변동 미반영. downtrend 아웃라이어 2건이
   정확히 이 케이스(하락기 진입 → 회복기 보유).
5. **KR 단일 시장·2021–2024** — 타 시장/기간 일반화엔 추가 표본.
6. (실행 중 추가) 가격 윈도가 2025-06-30까지라 2024 말 watch 에서 2025년 진입
   트레이드 7건 포함, 그중 3건은 윈도 끝 미청산(open) — 국면 라벨·집계엔 포함되나
   최종 P&L 은 절단값.

## 결론

- **1차(사전등록 §7.1): PASS** — 하락·조정기 1,075 분류점에서 entry 0건. §3.5 시장
  게이트의 분류층 방어는 4년 · 무작위 100종목에서 완벽하게 작동했다.
- **보조(§7.2): 미충족이나 사전등록 해석 규칙상 게이트 실패 아님** — 비게이팅 결정론
  근사의 하한 손실이며, correction 국면 손실은 대부분 한 자릿수 손절로 통제된 whipsaw.
- 수익성 자체(승률 24%, 꼬리 의존)는 이 설계의 판정 대상이 아니며 표본도 부족 —
  주장하지 않는다.

## 재현

```
uv run python -m kr_pipeline.backtest.profitability_cli analyze   # 주 결과(동결 100)
uv run python /tmp/bt_gapcheck.py                                 # 커버리지 C=0 확인
uv run python /tmp/bt_sensitivity.py                              # 민감도(+드리프트 14)
```
민감도 로직: `FROZEN_SAMPLE + (적재 distinct − FROZEN_SAMPLE)` 를 `run_analysis` 에 전달.
