> **[기록 문서]** 본 문서의 규약 문장은 작성 시점 기록이며, 현행 규칙은 `docs/superpowers/governance.md` 절 ID를 따른다 (#155, 2026-09-09).

# #114 경로 B — DART 구조화 공시 기반 v3 재구성 작업 계획

> 전문가 6차 승인(2026-08-19) 이행 계획. 상태: 착수 대기.
> 설계 원문: specs/2026-08-18-issue114-delisted-restore-design.md §4.5 + #114 6차 코멘트.

## 목표

증자·감자·분할류의 **기준일(→권리락일)·비율·발행가**를 DART 구조화 엔드포인트에서
직취해 v3 재구성 → 재측정 → 잔여 건 분류 → 동결 v3에 허용치 봉인.

## Phase 1 — DART 구조화 엔드포인트 검증·클라이언트 확장

- 대상 엔드포인트(주요사항보고서 주요정보 계열, 실명은 소량 실호출로 확정):
  유상증자결정(piicDecsn — `ic_mthn` 증자방식으로 주주배정/3자배정/일반공모 판별),
  무상증자결정(fricDecsn), 유무상증자결정, 감자결정(crDecsn), 주식분할·병합결정.
- `dart_client.py` 에 엔드포인트별 fetch 함수 추가(기존 페이싱·재시도 관례 승계).
- 신규 테이블 `corp_action_details`(ticker, rcept_no, action_type, record_date
  기준일, ratio, issue_price, method 배정방식, disclosed_at) — schema.sql + 양 DB.
- 소량 검증: 검증 범위 종목 3~5개의 알려진 이벤트(v2 매칭분)와 대조.

## Phase 2 — 백필 (검증 범위 1,443 + 상폐 435)

- 티커→corp_code 매핑은 기존 corp_code_sync 재사용(상폐 종목 corp_code 존재
  여부 확인 필요 — DART 는 상폐 후에도 corp_code 유지 추정, Phase 1 에서 검증).
- DART 쿼터(일 2만) 내 배치 — KRX 무관.

## Phase 3 — v3 재구성 + 재측정

- v3 = v2 + corp_action_details: 소형 조정의 시점 = 기준일−1영업일(권리락),
  배율 = 유상(TERP 공식: 발행가 반영)·무상(1/(1+비율))·감자(비율) 직접 계산.
  3자배정·일반공모(권리락 없음 유형) = 이벤트 제외 → FP 차단.
- 기존 오차 리포트 러너 재사용(--v3) → 오차 분포.

## Phase 4 — 잔여 건 분류 (전문가 6차 ③)

- 대형 41건: DART 행위 유형 전수 분류 → (a) v3 반영 / (b) 품질 플래그 층화 /
  (c) 데이터 결함 별도 등록.
- FP 47건: DART 원공시 심판(5차 ②) — pykrx 누락은 DB 결함 등록 + FP 제외.
- 소형 잔존분: 품질 플래그 층화.

## Phase 5 — 커버리지 표 + 허용치 봉인 (전문가 6차 ①②)

- 기업행위 커버리지 표: {유상증자(주주배정/일반공모), 무상증자, 유상·무상감자,
  액면분할/병합, 합병·분할, 주식배당} × {v2 / 경로B / 미처리}.
- 허용치 제안(41·47 분류 반영 후 분포 기준) → 방법론 세션 합의 → **동결 v3 에
  봉인 + 이후 파이프라인 수정 시 재검증 자동 발동 조항** 명기.

## 규율

- DART 호출만(KRX 0). 기존 데이터 무수정 원칙 유지(신규 테이블 + 신규 행만).
- 코드 변경은 리뷰 후 머지(레포 관례), 상폐 종목 지표 투입 금지는 허용치 통과까지.

## 직렬 체인 고정 (전문가 8차, 2026-08-19 — 실행 전 문서화·중도 변경 금지)

1. **v4** — 파싱 소스 교체만: 주주배정(·주주우선공모) 유상증자의 신주배정기준일·
   발행가를 DART 공시 원문(document API)에서 추출 → detail 편입.
2. **v4.1** — 매칭 지표에 크기 정합 조건 추가:
   **허용 오차 = |log(재구성 배율) − log(참조 배율)| ≤ log(1.15)** [design-judgment]
   — 근거: 조정일 하루의 통상 시세 변동과 유상 TERP 근사 잔차를 흡수하는 최소폭.
   결과 보며 조정 금지.
3. **v4.2** — 감자 crDecsn 기준일을 detail 경로로 사용(현행 라벨 전용 → 이벤트화).

- 각 단계 사이 동일 지표 측정: 대형 잔여 / FP / p99 중앙.
- 허용치 봉인 = 체인 최종 버전(v4.2) 동결 후 1회 — 중간 버전 봉인 금지.
  재검증 자동 발동 조항은 최종 버전에 명기.

## v4.2′ 정지 규칙 (전문가 9차 — 실행 전 봉인)

- 지표: {대형 잔여, FP, p99 중앙, p90} + **감자 클래스 오차**(crDecsn 기준일
  보유 종목의 ticker-p99 중앙값).
- 판정: v4.2′가 v4.1 대비 **전 지표 비악화 AND 감자 클래스 개선** → v4.2′ 동결.
  하나라도 악화 → **v4.1 동결** + 감자 detail 은 층화 후보로 이동. **v4.3 없음.**
- 메커니즘(9차 ④ 우선 검토 채택): **정지 스팬 앵커 매칭** — cr detail(기준일 rd)에
  대해 [rd−7일, rd+W] 안에 대형 갭 이벤트가 존재하면 detail 스킵(같은 사건 —
  대형 경로 전담). W 는 아래 실분포로 도출[measurement-based]. 스코프 = crDecsn
  경로 한정(전역 ±3일 불변). 확장으로 새로 병합되는 쌍 수 보고 + 표본 점검(③).

## 세그먼트 사용 정책 (전문가 10차 (b) 승인 — 사전 봉인)

- **미해결 이벤트 클래스(봉인)**: {ratio_mismatch, 복합(merger/spinoff),
  원문미제공(rights_no_doc), no_signal(조사 종결 전), FP 확정 건}.
- 종목별 **사용 구간 = 마지막 미해결 이벤트 이후** (+ 지표 워밍업 차감).
- **자동 연장 규칙**: 위 클래스의 건이 후속 조사·수정으로 해결되면 목록에서
  제거하고 세그먼트 시작점을 그 이전의 미해결 이벤트로 자동 갱신(재측정 동반).
- 채택 게이트(①): 실사용 구간의 오차 분포가 반올림·호가 수준으로 붕괴 확인 시
  확정. **허용치 봉인 대상 = 실사용 데이터 오차 분포**(전체 시계열 아님).
- (a) 병행: 종목 p99 + 플래그 층화 — 측정 대상만 실사용 구간으로 교체.

## 11차 결정 봉인 (전문가 11차, 2026-08-19 — 게이트 확정·주식배당 처분·범위 축소)

측정 근거: 잔여 27% 가설 판정(`scripts/issue114_seam_probe.py`, 커밋 da8c407) —
이음새 기각, 주식배당 확증(186/190 주식수 정합), 원인 3분해.

### ① 게이트 확정 — 세그먼트 정책 채택. 허용치 봉인의 클래스별 처분:

- 근사 잔차 226종목 + 소형 고립 36종목 → **허용치 흡수**(규명·상한 확인된 잔여 오차).
- 주식배당 64종목(순수 55+혼합 9) → **흡수 금지, carve-out**: 허용치 분포에서 제외,
  플래그 층화("기지 계통 클래스 — v5 정확 처리 예정" 명기). '알려진 한계' 흡수 형식 불가.

### ② 주식배당 = (a)+(c) 직렬, (b) 절단 기각

- (b) 기각 사유: 연례 반복 이벤트 → 상습 배당 종목 세그먼트가 매년 재절단되어
  실효 구간 0 수렴. 절단 정책은 반복 클래스에 부적합.
- **v5 직렬 체인 사전 확약(지금 봉인, 실행 전 중도 변경 금지)**:
  - 선행 조건: **상폐 재구성(P0 생산) 착수 전 필수 선행** — 참조 부재 상폐 구간에서는
    이 실명(주식배당 미반영)이 검증 불가능한 생산 오류가 되기 때문.
  - 체인 정의: v5 = 동결 v4.1 + 주식배당 detail 편입(무상증자 동형).
    시점 = 배당기준일 직전 거래일(권리락), **배율 = 1/(1+주당 배당주수)**.
    식별 = DART 주식배당결정 공시(엔드포인트 실명은 소량 실호출로 확정 — 비순환 식별).
  - **표적 재검증 범위 = 주식배당 64종목만**(전 범위 재실행 아님).
  - 정지 규칙: 표적 64종목 ticker-p99 중앙 개선 AND 기존 3지표(대형 잔여·FP·p99 중앙)
    비악화 → v5 동결. 하나라도 악화 → v4.1+(a) 층화 잔류. v5.1 없음.

### 허용치 봉인 (①·② (a) 이행 실측 — issue114_seam_probe_20260819.json carveout_stockdiv)

- 대상 = 실사용 구간 오차 분포, 주식배당 64종목 carve-out 후: **1,190종목·1,720,853 종목-일**.
- 종목 p99: **중앙 0.0% / p90 11.2% / ≤0.1% 69.1% / ≤1% 76.9%** (pooled p50 0.0%·p90 2.7%·
  p99 31.2%·max 1400% — 흡수 클래스(근사 잔차·소형 고립)의 규명된 꼬리, 층화 플래그로 노출).
- 소비 규칙: 백테스트 투입 시 종목 p99 층화 플래그 동반(>1% 종목 분리 보고 가능해야 함),
  carve-out 64종목은 v5 처리 전 투입 금지.
- **재검증 자동 발동 조항**: `adj_reconstruct.py` 파이프라인 수정 시 본 분포 재측정 의무
  (러너 `scripts/issue114_seam_probe.py`).

### v5 실행·정지 규칙 판정 (2026-08-20) — **v5 동결 확정**

- 소스 확정: 구조화 엔드포인트 부재(alotMatter 는 라벨 신뢰 불가 실측) →
  수시공시 "주식배당결정"(pblntf_ty='I') 원문 파싱(`parse_stkdp`). 백필 271행·62종목
  (원문미제공 26건 — rights_no_doc 동형 층화, 주총 후 정정 4건 제외).
- 봉인 공식의 실측 정밀화 2건 [measurement-based, 표적 재검증으로 사후 확인]:
  ① 배율 = 1/(1+**배당주식총수/발행주식총수**) (1주당 기반은 자기주식만큼 과대 —
  KRX 기준가 조정 실측 정합), ② 연말 기준일(12/28~31)의 락일 = 폐장일 **직전** 거래일
  (비연말은 무상 동형 k-1). + 기준일 후 접수(주총 정정) 제외, 타당성 한계 0<r≤0.5
  (기재정정 2단 표 오정렬 3건 실측 차단).
- **정지 규칙 판정 (동일 데이터 0820, v4.1 vs v5)**:
  표적 64종목 p99 중앙 7.96% → 48종목 ≤1% 이탈(중앙 ≤1%) **개선** /
  대형 잔여 176→176 **동일** / FP 30→30 **동일** / p99 중앙 0.1135%→0.0934% **개선**
  → 4조건 충족, v5 동결(`v3_events` 기본 `use_stkdp=True`).
- 잔여 악화 외양 2건(101240·187870)은 **상쇄 해제**로 확증 — v5가 배당 미스매치를
  제거하자 기존 흡수 클래스(유상 갭 근사 잔차·참조 부재 이벤트)가 노출된 것.
  v5 유래 결함 0.
- **허용치 갱신(동결 v5 기준)**: carve-out 잔존 = {001040(원문미제공),
  214390(2024-12-30 30.9% — 주식배당 공시 부재, 미해결)} 2종목뿐 →
  **1,252종목·1,848,412 종목-일: 종목 p99 중앙 0.0% / p90 10.5% / ≤1% 77.0%**.
  소비 규칙·재검증 자동 발동 조항은 위 봉인분 유지(carve 64→2 로 축소).

### ③ 범위 축소 — 조건부 승인

- no_signal 12·other 22 → **백로그 이관 승인**. 조건:
  (i) 조사 종결 전 절단 목록에서 제거 금지(현 봉인 유지 — 자동 연장 규칙의 "해결" 요건
      은 조사 종결로만 충족).
  (ii) **v5/상폐 재구성 착수 시점 재검토 태그** — 미지 클래스는 참조 없는 구간에서
      자가 검출 불가, 그 시점에 조사 가치 재상승.
- pykrx 누락 3건(221800·232830·388610) 소급 오염 점검: **이관 불가, 즉시 실행**
  (운영 판정 정합성 사안, 비용 = 저장 로그 조회).
- pykrx×DART 전 유니버스 대조 백로그 등록 여부: 사용자 결정 대기 유지.
